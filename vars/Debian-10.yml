---
# role: ansible-role-postgresql
# file: vars/Debian-10.yml

postgresql_version: "11"

postgresql_daemon: "postgresql@{{ postgresql_version }}-main"

postgresql_python_libs:
  - python3-psycopg2
  - python3-ipaddress
